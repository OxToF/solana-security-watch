// Edit these two lines after you deploy, then push (Vercel auto-redeploys).
//
//   SSW_ENDPOINT: your backend's /scan URL. While null, the form falls back to a
//                 prefilled email (mailto) so you still capture leads.
//   SSW_CONTACT : the email that receives lead/watch requests via mailto.
window.SSW_ENDPOINT = null;                    // e.g. "https://solana-security-watchdog-scan.fly.dev/scan"
window.SSW_CONTACT  = "solanawatchdog@proton.me";
