// Edit these two lines after you deploy, then re-upload the site. Loaded before
// the page script so it can read them.
//
//   SSW_ENDPOINT: your backend's /scan URL. While null, the form falls back to a
//                 prefilled email (mailto) so you still capture leads.
//   SSW_CONTACT : the email that receives lead/veille requests via mailto.
window.SSW_ENDPOINT = null;                 // e.g. "https://solana-security-watch-scan.fly.dev/scan"
window.SSW_CONTACT  = "CONTACT@EXEMPLE.COM"; // e.g. "hello@yourdomain.com"
